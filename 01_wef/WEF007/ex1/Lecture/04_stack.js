const arr = [];
arr.push[10];
arr.push[15];
arr.push[20];

//Perform pop remove last item and return the first item
const lastBook = arr.pop();
console.log(lastBook);
console.log(arr);
//Perform Object to protect inner info
function createStack()